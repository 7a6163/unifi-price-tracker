name = "unifi-price-tracker"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[triggers]
crons = ["0 */6 * * *"]

[[d1_databases]]
binding = "DB"
database_name = "unifi-price-tracker"
database_id = "REPLACE_AFTER_d1_create"

[vars]
# Non-secret config. Secrets (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID) set via `wrangler secret put`.
STORE_BASE = "https://tw.store.ui.com"
